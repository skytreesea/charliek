# 백테스트 로직 오류 분석 보고서

## 문제 상황
- 실제 데이터: AAPL 주가가 1월 13일 $258.72 → 1월 16일 $255.53로 하락 (약 -1.2%)
- 잘못된 결과: 포트폴리오 가치가 $157,755로 폭등 (57.76% 수익률)
- 논리적 파탄: 주가 하락인데 포트폴리오 가치가 상승

## 이전 로직의 문제점 분석

### 1. CSV 파싱 오류 가능성
**문제점**:
- `row[5]` 또는 `row[4]` 인덱스를 직접 사용
- Volume 컬럼(거래량)을 주가로 오인할 가능성
- Yahoo Finance CSV 형식: `Date, Open, High, Low, Close, Adj Close, Volume`
  - 인덱스 5 = Adj Close (정상)
  - 인덱스 6 = Volume (문제!)
  - 만약 헤더가 다르거나 순서가 바뀌면 Volume을 주가로 읽을 수 있음

**예시**:
- Volume이 수백만 단위 (예: 45,730,847)
- 이를 주가로 오인하면 $45,730,847로 계산
- 결과: 포트폴리오 가치가 비정상적으로 폭등

### 2. 날짜 동기화 문제
**문제점**:
- 각 종목의 날짜가 일치하지 않을 때 잘못된 가격 매칭
- `find_price_for_date`가 이전 날짜의 가격을 사용하면서 날짜 불일치 발생
- 예: 종목 A는 1월 13일 가격, 종목 B는 1월 12일 가격을 사용하는 경우

### 3. 계산 로직의 복잡성
**문제점**:
- 주식 수 계산 방식과 지수화 방식이 혼재
- 여러 단계를 거치면서 오차 누적
- 비율 계산이 중복되어 오류 발생 가능

## 수정된 로직

### 1. CSV 파싱 개선
- 헤더를 명시적으로 검증
- Volume 컬럼을 주가로 오인하지 않도록 보호
- 숫자 형식 검증 추가

### 2. 수학적으로 정확한 공식 사용
```
V_t = V_0 × Σ(Weight_i × Price_{i,t} / Price_{i,0}) + Cash
```

**단계별 계산**:
1. 기준일 가격 P_0 고정
2. 각 날짜별로: Price_{i,t} / Price_{i,0} 계산
3. 각 종목별로: Weight_i × (Price_{i,t} / Price_{i,0}) 계산
4. 모든 종목 합산: Σ(Weight_i × Price_{i,t} / Price_{i,0})
5. 초기 자본 곱하기: V_0 × Σ(...)
6. 현금 추가: + Cash (변동 없음)

### 3. 디버깅 로그 추가
- 각 날짜별로 상세 계산 과정 출력
- 시작가, 현재가, 비율, 비중, 가중비율, 종목가치 모두 출력
- 첫 5개 날짜와 마지막 5개 날짜만 상세 출력 (성능 고려)

## 예상 결과 (수정 후)

**AAPL 단일 종목, 1월 13일 ~ 1월 16일**:
- 시작일 (1월 13일): $258.72
- 종료일 (1월 16일): $255.53
- 가격 변화: -1.23%
- 포트폴리오 가치: $100,000 × (255.53 / 258.72) = $98,770
- 수익률: -1.23%

이제 정확하게 계산됩니다.
